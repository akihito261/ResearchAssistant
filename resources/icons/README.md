# Research Assistant icons

This directory contains the application's small, project-owned outline icon set.
Every icon uses the same 24 x 24 view box, rounded strokes, and `currentColor` so
the shared Qt icon provider and the PDF.js skin can apply neutral, active, and
disabled colors consistently. The SVG assets are intentionally dependency-free
and remain sharp at Windows display scaling levels.

